name: Deploy Calculator

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 159.198.47.177
          username: beno
          password: 'gx42l!IEc#'
          protocol: ftps
          local-dir: ./
          server-dir: /public_html/
